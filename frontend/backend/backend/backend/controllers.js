// backend/controllers.js

function createReport(issueData) {
    // Validate issue data: title, description, images
    // Check location within 3–5 km
    // Return mock success
}

module.exports = { createReport };
